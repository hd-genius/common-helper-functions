/**
 *
 * @param {number} value the value to test
 * @returns {boolean} true if the value is even, false if the value is not
 */
// eslint-disable-next-line eqeqeq
exports.isEven = (value) => value != 0 && value % 2 === 0
